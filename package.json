{
  "name": "okazcar",
  "version": "1.2.0",
  "description": "OKazCar - Analyse de confiance pour annonces auto",
  "private": true,
  "scripts": {
    "build:ext": "node extension/build.js",
    "build:ext:prod": "node extension/build.js",
    "render:publish-db": "python3 scripts/publish_render_release.py --version auto",
    "test:extension": "vitest run --config vitest.config.mjs",
    "verify": "bash scripts/verify.sh",
    "version:patch": "npm version patch --no-git-tag-version && node scripts/update-version.mjs patch",
    "version:minor": "npm version minor --no-git-tag-version && node scripts/update-version.mjs minor",
    "version:major": "npm version major --no-git-tag-version && node scripts/update-version.mjs major",
    "version:tag": "git tag -a v$(node -p \"require('./package.json').version\") -m \"Release v$(node -p \"require('./package.json').version\")\"",
    "release:patch": "npm run version:patch && npm run version:tag",
    "release:minor": "npm run version:minor && npm run version:tag",
    "release:major": "npm run version:major && npm run version:tag"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "jsdom": "^25.0.0",
    "sharp": "^0.34.5",
    "vitest": "^3.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/malikkaraoui/Co-Pilot.git"
  },
  "author": "Malik Karaoui",
  "license": "UNLICENSED"
}
